# Anytime Markdown

**いつでも、どこでも、美しく書く。**

`/` を入力してスラッシュコマンドを試してみてください。\
`/table` でテーブル、`/mermaid` で図、`/markdownAll` で全機能テンプレートを挿入できます。

ツールバー右側のボタンで **編集** / **ソース** / **レビュー** モードを切り替えられます。\
右端のパネルからアウトライン、コメント、設定を開けます。
