export interface EditorSettings {
  fontSize: number;
  lineHeight: number;
  editorMaxWidth: number;
}

export const DEFAULT_SETTINGS: EditorSettings = {
  fontSize: 0,
  lineHeight: 1.6,
  editorMaxWidth: 0,
};
