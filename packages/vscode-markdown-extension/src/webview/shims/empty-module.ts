// Stub module for libraries excluded from the VS Code webview bundle.
// Dynamic imports of these libraries will reject, and the UI hides
// the relevant features via hideGraph context.
export default {};
